#include<stdio.h>
/**
 * main - A program that print single digit of base 10
 * return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d ", i);
	}
	return (0);
}
