#include"main.h"
/**
 * _isdigit - ckech if charactor is a digit
 * @c: The charactor to be used
 * Return: 1 for charactor that will be digit or 0 for any elsee
 */
int _isdigit (int c)
{
	if (c >= 48 && c <= 56)
	{
	return (1);
	}
	return (0);
}
